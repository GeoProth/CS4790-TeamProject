@model IEnumerable<CS4790TeamProject.Models.ViewModels.AssemblyRecipeViewModel>
@using CS4790TeamProject.Models;
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Assembly</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Assembly Item
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.items.ItemName)
            </td>
            <td>
                @*
                <a asp-action="Edit" asp-route-id="@item.AssemblyRecipe.FirstOrDefault(r => r.AssemblyRecipeId == Model.)">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AssemblyRecipeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AssemblyRecipeId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
